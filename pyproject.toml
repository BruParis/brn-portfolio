[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "brn-portfolio"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "yfinance",
    "pandas",
    "numpy",
    "scipy",
    "plotly",
    "pyarrow",
]
